# Checkpoints

Pretrained OmniGCD / GCDformer weights will be released here or linked from the main README.

For now, you can train a small synthetic checkpoint with:

```bash
python scripts/train_gcdformer.py --config configs/train_gcdformer.yaml --output checkpoints/gcdformer_synthetic.pt
```
